services:
  bot:
    build: .
    restart: always
    env_file:
      - .env
    depends_on:
      - db

  db:
    image: postgres:18
    restart: always
    env_file:
      - .env
    volumes:
      - pg_data:/var/lib/postgresql/data

volumes:
  pg_data:
